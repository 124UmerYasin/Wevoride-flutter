<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        
        <!-- Modern edge-to-edge support - replaces deprecated status bar APIs -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Enable edge-to-edge layout -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">shortEdges</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="s">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="s">false</item>
        
        <!-- Large screen and resizable activity support -->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">false</item>
        <item name="android:windowActivityTransitions">true</item>
        
        <!-- Material3 BottomSheet edge-to-edge support -->
        <item name="bottomSheetDialogTheme">@style/CustomBottomSheetDialog</item>
    </style>
    
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        
        <!-- Modern edge-to-edge support - replaces deprecated status bar APIs -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Enable edge-to-edge layout -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">shortEdges</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="s">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="s">false</item>
        
        <!-- Large screen and resizable activity support -->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">false</item>
        <item name="android:windowActivityTransitions">true</item>
        
        <!-- Material3 BottomSheet edge-to-edge support -->
        <item name="bottomSheetDialogTheme">@style/CustomBottomSheetDialog</item>
    </style>
    
    <!-- Custom BottomSheet Dialog with edge-to-edge support -->
    <style name="CustomBottomSheetDialog" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">shortEdges</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="s">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="s">false</item>
        <item name="enableEdgeToEdge">true</item>
    </style>
</resources>
